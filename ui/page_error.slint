import { VerticalBox } from "std-widgets.slint";

export component PageError inherits VerticalBox {
    in property<string> error: "";

    height: 100px;
    width: 100%;
    Text {
        text: "Произошла ошибка:";
        font-size: 20px;
    }
    Text {
        text: error;
    }
}
